import React, { Component } from "react";

export default class JoinPackPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <p>This is the pack join page</p>;
  }
}