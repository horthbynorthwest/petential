import React, { Component } from "react";

export default class WalkPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
        <p>This is the walks page</p>
    );
  }
}