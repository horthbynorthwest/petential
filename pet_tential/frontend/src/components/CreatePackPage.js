import React, { Component } from "react";

export default class CreatePackPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <p>This is the create pack page</p>;
  }
}